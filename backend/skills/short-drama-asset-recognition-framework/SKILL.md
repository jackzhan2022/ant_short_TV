---
name: short-drama-asset-recognition-framework
description: Use when recognizing formal characters, looks, physical scenes, props, and prop states from one current short-drama episode.
---

# 角色场景道具识别框架

本 Skill 只识别 `read_current_episode` 返回的当前剧集事实，并复用其紧凑资产目录。输出范围仅为 `characters`、`characterLooks`、`scenes`、`props`、`propVariants`；不得输出人物关系，也不得输出道具间的衍生、父子、组成或其他关系。

## 确定性身份匹配

资产目录仅为本集候选摘要，未列出不代表不存在。新建前必须调用
`search_script_assets` 按规范名和明确别名检索；命中后复用可信 key。
形态详情通过 `read_asset_details` 读取，`hasMore` 为 true 时使用
`nextCursor` 续页。摘要和详情的 `hasPrompt` 指示已保存提示词，
无需重新生成已有非空提示词（除非明确指定 REGENERATE_ALL）。
读取当前剧集后可多次检索与读取详情，最后只通过正式保存工具写入。

按以下顺序判断，不能跳级：

1. 当前目录中已有 `assetKey` 且本集证据明确指向该身份时，原样复用这个不透明 key。
2. 没有可直接引用的 key 时，只允许按规范名精确匹配同类型资产。
3. 规范名未命中时，只允许按目录中显式别名精确匹配。
4. 都未命中才创建 Schema 规定的运行内新身份 key。

不得用编辑距离、读音、单字相似、常识或模糊相似度自动合并。两个候选具有同一规范名或别名且正文不能消歧时，不得任选第一个，也不得新建第三个来绕过歧义；提交可验证事实，让 `save_episode_assets` 返回安全的不透明候选 key，整次保存失败后仅在本集证据足以消歧时修正。

同一人物的姓名、昵称和明确称谓不拆成多个人，选最稳定的原文姓名作为规范名，其余保存为别名。换装不能创建新角色。

## 资产与形态边界

- `characters`：剧中实际行动、说话或被明确指认的人物。
- `characterLooks` / 角色变装：只有服装、妆发、伤痕、年龄阶段等制作上可见且有持续识别价值的差异才形成角色形态。情绪、表情、姿势、动作或仅“整理头发”不形成变装。
- `scenes`：按物理地点建立稳定场景。清晨/深夜、晴雨、灯光和气氛变化不创建新地点。
- `props`：具有剧情、动作或视觉连续性价值的物件。通用同类道具由不同角色明确持有时保持不同身份，例如“林小满的手机”和“顾言的手机”不得合并。
- `propVariants`：同一道具制作上可见的不同形态或状态，例如完整与断裂。状态不得创建第二个逻辑道具。

只支持同一道具的不同形态。不得输出 `derivedFrom`、`parentPropKey`、`sourcePropKey`、components、relations 等衍生道具或道具关系。玻璃瓶破碎后，玻璃片若在本集被明确独立使用，可以凭自身证据成为独立 `prop`，但不能携带它与玻璃瓶之间的父子、来源、组成或容器关系。

## 资产生成提示词

当 `save_episode_assets` 的当前输入 Schema 提供 `prompt` 字段时，必须在识别资产的同一次调用中生成提示词。提示词是制作阶段的创作结果，**提示词不是证据**：`evidence` / `evidenceRef` 仍须逐字引用当前剧集；`prompt` 可以整合当前剧集事实、可信资产目录和冻结的全局设定，并合理补全未定义的视觉细节，但不得与已知事实冲突，也不得把补全内容写回证据。

生成时遵循以下优先级：当前剧集明确事实 > 可信资产目录既有设定 > 运行上下文实际提供的冻结时代与题材设定 > 不冲突的视觉补全。目录的 `hasPrompt=false` 表示该已有资产或形态缺少提示词，必须随本次 `save_episode_assets` 提交新 `prompt`；`hasPrompt=true` 表示提示词已建立，只复用稳定 key，**已有非空提示词不得覆盖**，也不得复述或提交替换提示词。新建记录同样必须提交 `prompt`。国籍/朝代、人种、性别和年龄属于身份敏感字段：没有可靠依据时填写“未明确”，不得用姓名、语言、职业或刻板印象猜测；脸型、发型、耳饰、身材、头身比和服装细节属于制作字段，缺失时可以进行符合时代、题材和角色身份的视觉补全。视觉补全只补足生成所需的表现细节，不得改变资产的核心几何、场景空间拓扑、明确材质、归属关系或剧情功能。

### 角色主体 `characters[].prompt`

输出 Markdown 字符串。任务段固定为：

```markdown
## 任务：完成角色的上半身正面平视特写和该角色的全身三视图，左边是角色的上半身正面平视特写，右边是该角色的全身三视图。三视图不可以有分割线，左侧为角色胸部以上特写大图，占画面约 40% 宽度，用于展示面部、发型、表情、眼神、上半身服装和配饰细节，右侧为同一角色的三视图，占画面约 60% 宽度，依次展示正面全身、侧面全身、背面全身。

角色描述:

- **时代基底**: {值}
- **国家/朝代**: {值}
- **人种**: {值}
- **类型基底**: {值}
- **脸型**: {值}
- **发型**: {值}
- **耳饰**: {值或无}
- **身材**: {值}
- **头身比**: {值}
- **上身着装**: {值}
- **下身着装**: {值}
- **鞋子**: {值}
- **性别**: {值}
- **年龄**: {值或年龄段}
```

身份事实和剧情明确的服装不得改写；脸型、身材、头身比等制作字段缺失时，可以按角色身份、时代和题材完成一致的视觉设计。最终提示词不得保留 `{值}` 占位符。

当前剧集只有一个可见造型且资产目录没有不同主体形象时，把该造型作为角色主体服装，不得重复创建内容相同的 `characterLooks`。只有目录已有不同主体形象，或本集明确发生持续可见的换装、妆发、伤痕或年龄阶段变化时，才输出角色变装。

### 角色变装 `characterLooks[].prompt`

只输出相对主体形象的视觉增量，不重复主体外貌，不添加构图、画风或一致性套话。允许保留模型执行变装所需且有可靠依据的最小身份锚点，例如性别；其余字段必须是本次形态变化。使用分号连接紧凑字段，例如：

```text
性别:女；衣着描述:白色连衣裙，裙摆湿透，赤脚
```

### 场景主体 `scenes[].prompt`

输出固定任务与场景描述组成的 Markdown 字符串：

```markdown
生成四宫格画面，展示同一个场景中的四个不同视角。左上角为正视图，主体正面清晰可见，构图居中，细节完整；右上角为俯视图，从高空俯视整体空间布局，展示环境关系和场景结构；左下角为背视图，从主体后方观察，突出背部轮廓、空间纵深和环境延展；右下角为侧视图，从主体侧面观察，展示主体比例、层次和空间关系。四个画面保持同一场景、同一光照、同一色调、同一时间状态。不输出文字信息。

1. 只出现场景，不出现人物、道具等无关内容
2. 必须只展示静态事物，不能包含人、动物等可以自行运行的事物
3. 无动态、特效、技能、光效及战斗相关描写

场景描述: {完整描述物理空间、固定陈设、材质、光照和空间关系}
```

四个视角必须保持同一地点和同一状态；固定门窗、灯具、地面和建筑构件属于场景，不作为剧情道具添加。

### 道具主体 `props[].prompt`

只描述道具本体，不添加人物、动作、场景、镜头或通用构图后缀。使用连续文本描述名称、整体结构、比例、材质、颜色、表面工艺、局部装饰、边缘和背面，例如：

```text
双环交叠徽章，由一大一小两个圆环以偏心方式相互嵌套构成，外层大环为深蓝色磨砂金属材质并带有细微拉丝纹理，内层小环为暖金色镜面金属，两环交叠处镶嵌扇形排列的半透明蓝金渐变鳞片，整体厚度均匀，边缘倒角精细，背面为平整金属底面。
```

`propVariants[].prompt` 与角色变装相同，只输出相对主体道具的可见状态增量。

### 完整输出示例（仅适用于 Schema 已声明 `prompt`）

以下示例展示字段职责；`evidence` 在真实调用中必须替换为当前剧集逐字原文：

```json
{
  "schemaVersion": 1,
  "characters": [{
    "localKey": "character_1",
    "assetKey": null,
    "name": "艾琳",
    "aliases": [],
    "evidence": "青年女子艾琳披着深蓝色睡袍，赤脚站在走廊里。",
    "prompt": "## 任务：完成角色的上半身正面平视特写和该角色的全身三视图，左边是角色的上半身正面平视特写，右边是该角色的全身三视图。三视图不可以有分割线，左侧为角色胸部以上特写大图，占画面约 40% 宽度，用于展示面部、发型、表情、眼神、上半身服装和配饰细节，右侧为同一角色的三视图，占画面约 60% 宽度，依次展示正面全身、侧面全身、背面全身。\n\n角色描述:\n\n- **时代基底**: 现代\n- **国家/朝代**: 未明确\n- **人种**: 未明确\n- **类型基底**: 奇幻\n- **脸型**: 鹅蛋脸，轮廓柔和\n- **发型**: 及腰金色微卷长发\n- **耳饰**: 无\n- **身材**: 身材高挑，体态修长\n- **头身比**: 7.5头身\n- **上身着装**: 深蓝色真丝睡袍\n- **下身着装**: 米白色吊带长裙\n- **鞋子**: 赤脚\n- **性别**: 女\n- **年龄**: 青年"
  }],
  "characterLooks": [{
    "localKey": "look_1",
    "characterLocalKey": "character_1",
    "variantKey": null,
    "name": "湿裙造型",
    "description": "白色连衣裙，裙摆湿透，赤脚",
    "evidence": "她穿着湿透的白色连衣裙，赤脚走进房间。",
    "preferred": true,
    "prompt": "性别:女；衣着描述:白色连衣裙，裙摆湿透，赤脚"
  }],
  "scenes": [{
    "localKey": "scene_1",
    "assetKey": null,
    "name": "风崖庄园走廊",
    "aliases": [],
    "description": "风崖庄园内部的一条昏暗走廊",
    "timeAtmosphere": "夜晚，昏暗",
    "evidence": "风崖庄园内部的昏暗走廊",
    "prompt": "生成四宫格画面，展示同一个场景中的四个不同视角。左上角为正视图，主体正面清晰可见，构图居中，细节完整；右上角为俯视图，从高空俯视整体空间布局，展示环境关系和场景结构；左下角为背视图，从主体后方观察，突出背部轮廓、空间纵深和环境延展；右下角为侧视图，从主体侧面观察，展示主体比例、层次和空间关系。四个画面保持同一场景、同一光照、同一色调、同一时间状态。不输出文字信息。\n\n1. 只出现场景，不出现人物、道具等无关内容\n2. 必须只展示静态事物，不能包含人、动物等可以自行运行的事物\n3. 无动态、特效、技能、光效及战斗相关描写\n\n场景描述: 风崖庄园内部的一条昏暗走廊，深色地毯从前景延伸至尽头的木门。"
  }],
  "props": [{
    "localKey": "prop_1",
    "assetKey": null,
    "name": "双环交叠徽章",
    "aliases": [],
    "ownerCharacterLocalKey": null,
    "description": "蓝金双色的双环交叠徽章",
    "evidence": "蓝金双色的双环交叠徽章",
    "prompt": "双环交叠徽章，由一大一小两个圆环以偏心方式相互嵌套构成，外层大环为深蓝色磨砂金属，内层小环为暖金色镜面金属，交叠处镶嵌扇形排列的蓝金渐变鳞片，边缘倒角精细，背面为平整金属底面。"
  }],
  "propVariants": []
}
```

如果当前 `save_episode_assets` Schema 尚未声明 `prompt`，不得提交未定义字段；先按现有 Schema 保存识别事实。上述提示词契约在 Schema 开放对应字段后生效。

## 证据与保存前检查

每个身份、别名和形态都要提供当前剧集中逐字出现的连续非空证据锚点；证据必须足以支持对应判断，不能使用行号、摘要、改写句、其他剧集或全局理解。不得从干扰文本识别本集未出现的资产。

保存前确认：key 类型和目录一致；匹配只经过已验证 key、精确规范名或精确别名；模型没有擅自解决同名歧义，也没有模糊自动合并；人物称谓、场景时间气氛、通用道具归属、角色变装和道具不同形态边界正确；没有道具衍生关系、业务标识或当前集外证据。若正文无法消除同名歧义，仍提交最小可验证事实以让 `save_episode_assets` 返回类型化歧义错误和安全候选，整次保存不得部分成功，Agent 不得声称完成。其他检查通过后正常保存，成功后立即结束。

调用 `save_episode_assets` 时，顶层必须始终提供 `characters`、`characterLooks`、`scenes`、`props`、`propVariants` 五个数组；本集没有某类资产时传空数组。每个角色、场景和道具必须显式提供 `aliases`，没有别名时传 `[]`。每个角色形态和道具形态必须显式提供布尔值 `preferred`。字段修复不能补写或改写证据；任何 `evidence` 都必须是当前集正文中逐字存在的连续文本。


<!-- asset-catalog-protocol:v1 -->
## 资产目录检索协议（v1）

read_current_episode 的资产目录仅为有界候选，不完整不表示资产不存在。
新建身份前调用 search_script_assets，按当前剧本中的规范名与明确别名检索；
有精确匹配时复用 assetKey，有歧义时不得猜测合并。
需要形态或可视设定时调用 read_asset_details，最多 10 个 key；形态每页最多 20 条。
hasMore 为 true 时按 nextCursor 续页；形态续页只传一个资产 key。
search_script_assets 每页最多 50 条；游标仅适用于当前运行、作用域和原查询。
响应预算为 32 KiB，超大详情错误应减少 key 数量或明确报告，不能伪装读取完整。
遵循 assetScope 和 assetPromptPolicy；按 read_current_episode → 可选检索/详情 →
save_episode_assets 的顺序执行，正式保存成功后立即结束。
