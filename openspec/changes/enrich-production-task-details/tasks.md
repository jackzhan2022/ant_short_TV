## 1. 来源核验与契约

- [ ] 1.1 为九类任务及六个现有 Script AI Operation 子类型建立字段级映射，记录输入/结果保存位置、固定版本、可变字段、缺失条件和对应业务读取/下载权限。
- [ ] 1.2 核验共享分镜执行、无项目审核稿、无 execution 历史任务和媒体存储引用，明确可读取边界及无法确认历史归属的字段；缺失不转化为新增写入需求。
- [ ] 1.3 定稿 TaskDetailContent、分区 kind/availability、来源版本与 contentRevision 契约，落实 4,000 字符预览、32,000 字符分块、20/100 项分页和 256 KiB 首屏上限。

## 2. 既有内容的只读解析

- [ ] 2.1 实现已保存任务字段、固定剧本/稿件版本、执行/run 与结果关联的读取策略；缺失时返回 NOT_RECORDED，禁止使用当前业务内容冒充历史。
- [ ] 2.2 复用已有版本/run/快照与受管理媒体引用，为新旧任务统一实现未记录、已删除及无法验证归属的提示；不新增快照存储、数据库迁移或媒体入库。
- [x] 2.3 保持创建、提交、调度、执行、回调、结果保存、控制和计费路径不变，不新增采集钩子或后台补录；验证 GET 不写回内容、不发起 AI 调用。
- [ ] 2.4 测试技术重试、重新生成、后续资产/剧本修改和结果覆盖，验证只返回可证明归属的已有内容，否则明确不可用；详情服务失败不影响原生产流程。

## 3. 内容端点与授权

- [ ] 3.1 增加选中任务 content 及分区分页/文本分块端点和固定适配器注册表，保留现有 list/summary/detail-state/control 响应兼容。
- [ ] 3.2 对正文、分块、媒体及下载组合当前任务可见性与领域资源授权，验证 tenant/task/version 归属；补齐无项目稿件和共享执行输入检查。
- [ ] 3.3 复用或补齐受控图片/缩略图/源视频/结果视频读取与 Range 支持，限制资源标识来源，沿用下载权限，不引入永久公开地址或任意 URL 代理。
- [ ] 3.4 实现分区缺失、未产出、删除、受限和不支持状态，允许非权限类局部失败保留其他结果；统一错误脱敏和用户可见字段白名单。
- [ ] 3.5 增加跨团队/跨成员/角色撤销/项目撤权/资源 ID 替换/无项目审核稿/共享分镜授权测试，覆盖输入、文本续读、缩略图、视频和下载。

## 4. 全类型内容适配

- [ ] 4.1 实现 IMAGE 的保存提示词、参考图、参数、任务所属结果画廊和当前采用标记，区分用户原始要求与已加工生成提示词。
- [ ] 4.2 实现 VIDEO 的提示词、首尾帧、其他参考素材、生成设置、任务视频与封面，核验历史失效媒体状态。
- [ ] 4.3 实现 SCRIPT_GENERATE / SCRIPT_REWRITE 的创作要求、输入版本/范围与本次结果版本正文，验证改写前后内容分别可读。
- [ ] 4.4 实现 ELEMENT_EXTRACT / SCOPED_ASSET_REEXTRACTION 的来源与处理范围、按本次任务关联的分类资产结果和属性，避免返回当前全项目资产替代历史结果。
- [ ] 4.5 实现 STORYBOARD_BREAKDOWN / PROMPT_GENERATE 的固定输入、生成要求、目标关联、本次分镜/提示词结果及警告。
- [ ] 4.6 实现 SCRIPT_ANALYSIS 的固定原文、范围、配置与真实阶段产物，覆盖分集/人物/场景/道具及无 execution 历史记录。
- [ ] 4.7 实现 REVIEW 的固定稿件、审核模式/范围/维度、本轮报告/结论/问题明细/建议，复用标准报告解析并验证不会读到最新轮次。
- [ ] 4.8 实现 VIDEO_DECOMPOSITION / VIDEO_EPISODE 的提交视频信息、处理设置、批次输入清单与单集分析/剧本结果。
- [ ] 4.9 实现 STORYBOARD_BATCH / STORYBOARD_ITEM 的批次输入范围、单集原文与结果，区分批次提交人和共享执行来源，覆盖空批次。
- [ ] 4.10 对全部适配器验证成功、进行中、失败/部分成功、新旧任务缺失记录及未知历史子类型；按字段映射提供真实可用输入/结果样本和缺失场景证据，不能只验收通用摘要，也不能为补足样本新增生产写入。

## 5. 任务页面与用户详情界面

- [ ] 5.1 查询所用 antd 组件 API，拆出详情专用组件和类型，实现概况/提交内容/设置/结果/业务阶段框架、按状态选择初始展开区及真实耗时标签。
- [ ] 5.2 实现长文本/提示词/报告的预览、分块展开、复制预览与复制全文，显示截断和来源版本，禁止直接执行返回的原始 HTML。
- [ ] 5.3 实现参考图与生成图画廊/放大、首尾帧标签、视频按需播放及有权限的下载；局部媒体失效不隐藏其他结果。
- [ ] 5.4 实现分镜、资产、分析阶段和审核问题等结构化结果展示与分页，复用对应业务的规范化格式，避免显示原始 JSON 或内部日志。
- [ ] 5.5 实现批次到子项与返回导航，URL 保留父任务、子项页、活动分区和列表查询；点击子项不加载兄弟项正文。
- [ ] 5.6 将内容加载与轻量状态轮询分离，完成首开/终态/显式刷新/已知内容版本变化时的更新；关闭、换任务或换团队取消请求、释放媒体并隔离迟到响应。
- [ ] 5.7 实现历史未记录、未生成、删除、受限、不支持和加载失败的分区反馈；权限变化后的响应必须移除失去授权的已显示内容。
- [ ] 5.8 参考套餐管理 packages/index.tsx，将任务页组织为 PageContainer、授权范围 Tabs、ProTable 搜索/工具栏/表格/分页；统一主次文本、状态 Tag、时间/进度、查看详情操作和空态，不复制套餐 CRUD 操作或扩大角色权限。
- [ ] 5.9 实现标准查询/重置及刷新，输入时不逐键请求，提交/重置回到第一页，保留 URL 和同条件统计；项目/创建人复用有界授权查找或已知标签，保持服务端默认 20、最大 100 的分页，禁止全量拉取后切片。
- [ ] 5.10 对齐套餐抽屉的标题、操作区和间距，桌面约 720px、窄屏适配视口，媒体自适应、宽表局部滚动，关闭后保留列表及批次上下文。

## 6. 回归与性能验证

- [ ] 6.1 增加前端交互测试，覆盖各类内容、成功优先结果、长文本全文复制、媒体按需加载与失败、批次返回定位、刷新恢复、任务/团队切换和权限撤销；验收套餐风格布局、查询/重置、服务端分页、权限范围及窄屏抽屉。
- [ ] 6.2 增加后端边界测试：超长原文、大结果集合、大批次、参数越界及首屏大小限制；证明列表不取正文、详情不逐行加载兄弟内容、GET 不写业务数据或新增执行/计费，并验证查询超时及并发限制、无锁定读取及无生产事务依赖。
- [ ] 6.3 运行相关领域与任务中心后端测试、前端测试、npm run lint、npx antd lint ./src 及构建，保留原控制、幂等和计费回归证据。
- [ ] 6.4 在部署环境 MySQL 对原列表及新增详情首屏/分区/分块执行查询计划、实际读取量、时延和响应大小验收；预先固定负载及回归预算，对比未打开详情与并发浏览时的业务提交/生产阶段延迟和吞吐，超预算不得通过；验证超时降级、新旧应用兼容及回滚，保留既有任务和结果数据。

## 7. 交付

- [ ] 7.1 完成全类型字段/场景覆盖矩阵和用户角度验收记录，明确历史缺失与同步能力边界；任何未完成类型保持任务未勾选。
- [ ] 7.2 更新只读接口、页面及部署/回滚说明，注明无新增数据库迁移、快照采集或业务写路径改造，记录正文权限与媒体读取边界，运行 OpenSpec 严格校验并核对规格场景。
