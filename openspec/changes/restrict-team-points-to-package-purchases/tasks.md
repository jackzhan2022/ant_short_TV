## 1. 后端移除手工调整能力

- [x] 1.1 搜索并确认所有 `/points/adjust` 与 `TeamPointService.adjust` 调用点，仅保留商业化权益发放对 `PointAccountingService.grant` 的调用
- [x] 1.2 删除 `TeamPointController` 的手工调整路由和 `TeamPointService.adjust` 实现及其无用依赖
- [x] 1.3 更新后端积分控制器测试，断言手工调整 URL 返回 404，并保留账户、流水、对账和 AI 消耗覆盖

## 2. 前端移除手工调整入口

- [x] 2.1 从团队设置页面移除“调整积分”弹窗、表单和 `adjustTeamPoints` 调用/import
- [x] 2.2 移除不再使用的前端积分调整服务导出，保留账户和流水查询服务
- [x] 2.3 更新团队设置测试，确认调整按钮不渲染且余额、统计和历史流水仍可展示

## 3. 商业化发放回归验证

- [x] 3.1 增加或更新商业化订单/订阅测试，验证支付后积分发放、账本关联和重复重试幂等
- [x] 3.2 验证历史手工调整流水仍能通过查询接口返回并由前端兼容展示

## 4. 质量检查

- [x] 4.1 运行后端相关 Maven 测试
- [x] 4.2 运行前端积分、团队设置和商业化页面测试
- [x] 4.3 运行项目 lint/type-check，确认无未使用导入或类型错误
