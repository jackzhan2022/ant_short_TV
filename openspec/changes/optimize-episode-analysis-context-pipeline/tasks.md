> 2026-09-12 替代声明：`remove-legacy-ai-workflow-paths` 是最终目标。LEGACY_V1、pipelineVersion、enabled 切流和旧流程回退条款已被替代；保留可信逐集上下文、独立持久化双分支和自动分镜。 原勾选和验证记录为历史证据，不据此恢复旧链路。归档时先合并仍有效的基础能力，最后合并 remove-legacy-ai-workflow-paths；之后不得再导入这些已退役条款。

## 1. 基线与契约确认

- [x] 1.1 核对现有分镜工作流、确定性校验、租约恢复及审核缓存实现，记录复用接口与未归档变更交点。
- [x] 1.2 建立匿名化 59 集/8 分组夹具、无标题及混合标题夹具；记录当前请求轮数、tokens、模型耗时与失败基线。

## 2. 分集粒度校验

- [x] 2.1 实现单集/分组/目录/引用标题分类及稳定原文偏移，覆盖中文数字、阿拉伯数字和缺号歧义测试。
- [x] 2.2 将可信标题结构加入 AI 拆分输入，保留全文与分块回退路径。
- [x] 2.3 在保存前加入单集边界粒度验证，拒绝 59 集合并为 8 组，并保留精确完整覆盖及来源哈希校验。
- [x] 2.4 验证稳定集 ID 对齐、旧集退休与下游成果保留，确保校验失败没有部分写入。

## 3. 冻结上下文与可信预加载

- [x] 3.1 添加上下文快照增量迁移及仓储，保存可恢复内容/不可变来源引用、哈希和协议版本，验证重启后内容一致。
- [x] 3.2 实现 EpisodePromptContextFactory，固定序列化公共规则、可选全局理解、正文和段落编号，排除概要及动态字段。
- [x] 3.3 调整 Runner 消息布局与阶段 Skill 后缀，验证同集三个阶段公共前缀完全相同、跨租户及来源变更隔离。
- [x] 3.4 为概要和识别实现可信预加载、runState 初始化和审计，修改读取/保存契约，验证首轮模型可直接保存。
- [x] 3.5 接入分镜现有预加载及来源覆盖校验，把动态资产、必要相邻集信息放到后缀，确认上下文不包含生成概要。
- [x] 3.6 稳定共享工具描述并保留阶段白名单，测试跨阶段调用、来源篡改、外租户资产与未保存结束均被拒绝。

## 4. 缓存与耗时观测

- [x] 4.1 复用供应商缓存参数适配，生成内容版本缓存键，测试同模型重试复用、异模型隔离及不支持参数的兼容路径。
- [x] 4.2 记录真实步骤起止、排队、准备、模型、校验保存和总耗时；关联阶段、集、上下文和调用日志。
- [x] 4.3 汇总已知缓存命中量及未知数量；首字节不可测时保留未知，测试零值与缺失值区别。

## 5. 独立分支与执行预算

- [x] 5.1 为新任务记录流水线版本，持久化拆分后概要/识别独立子任务，保留旧任务执行模式。
- [x] 5.2 调整父任务收口与异常传播，测试概要失败不阻断识别、部分分支运行时状态可恢复且不误报完成。
- [x] 5.3 实现按供应商/模型共享的多实例并发额度、可恢复领取与公平调度，验证排队不占运行预算。
- [x] 5.4 实现概要、识别、分镜独立总预算与请求上限，测试剩余预算裁剪、取消、租约丢失和阶段独立重试。

## 6. 资产完成后自动分镜

- [x] 6.1 新增持久化触发事件及唯一约束，在本集资产完成且 coverage 校验成功后事务记录事件。
- [x] 6.2 消费事件接入现有分镜任务、授权与积分预扣入口，冻结资产依据并记录来源关联。
- [x] 6.3 实现重复回调、并发手动生成、已存在分镜和人工编辑保护，验证同版本不会重复自动生成或重复扣费。
- [x] 6.4 实现过期事件失效、资金不足受阻及分镜独立重试；分镜失败不改变识别成功状态。
- [x] 6.5 验证保存后宕机、派发后宕机、服务重启可恢复，并确认全剧资产 finalizer 不破坏已生成分镜引用。

## 7. 工作台与兼容

- [x] 7.1 扩展接口提供逐集概要、识别、分镜独立状态、错误和任务链接，保持既有字段兼容。
- [x] 7.2 按当前 Ant Design API 更新工作台状态与重试入口，明确自动分镜及费用，测试部分失败、刷新恢复和人工保护提示。
- [x] 7.3 更新运行手册，说明开关、超时/并发、历史错误分集显式重解析，以及新版本任务排空后回滚程序的步骤。

## 8. 验证与发布准备

- [x] 8.1 运行分集、预加载、资产匹配、分镜覆盖、调度恢复、鉴权和积分相关后端测试，并验证新增迁移。
- [x] 8.2 运行前端测试、npm run lint、npx antd lint ./src 与生产构建，修复本变更引入的问题。
- [ ] 8.3 在测试环境验证正确分集后概要与识别独立推进、本集识别触发分镜、失败独立重试的端到端流程。
- [ ] 8.4 受控验证实际供应商缓存能力，对比调用轮数、输入总量、已知命中率、成本、P50/P95 和成功率；未知数据明确标注。
- [ ] 8.5 验证旧任务兼容及关闭开关/恢复演练，形成发布记录；按用户后续部署指令执行上线。
