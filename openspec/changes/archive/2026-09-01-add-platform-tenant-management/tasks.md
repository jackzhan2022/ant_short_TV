## 1. 数据库与平台权限

- [x] 1.1 新增 Flyway 迁移，为 `operation_log` 增加可空 `detail_json` 字段，并创建 `PLATFORM_TENANT_VIEW`、`PLATFORM_TENANT_STATUS_EDIT` 权限且默认授予平台超级管理员角色。
- [x] 1.2 扩展操作日志实体与服务以写入结构化详情，同时保持现有调用方兼容，并添加迁移及日志持久化测试。
- [x] 1.3 核对租户状态、有效成员、积分账户和团队订阅聚合查询所需索引，仅在现有索引不足时通过迁移补充。

## 2. 后端平台租户 API

- [x] 2.1 先添加平台租户控制器权限测试，覆盖有查看权限、仅查看权限、无权限和非目标租户成员访问场景。
- [x] 2.2 定义平台租户分页参数、列表摘要、Owner 摘要、套餐摘要和详情响应 DTO，并实现参数枚举与页大小校验。
- [x] 2.3 实现未软删除租户的关键字、状态、当前套餐类型筛选和分页查询，确保筛选后的总数准确。
- [x] 2.4 实现按当前页租户 ID 批量加载 Owner、有效成员数、积分余额和当前订阅/套餐信息，并添加无积分账户、无套餐及固定查询数量测试。
- [x] 2.5 实现租户聚合详情与待生效套餐查询，覆盖停用租户、不存在租户、排队顺序和异常套餐快照回退测试。
- [x] 2.6 实现 `GET /api/platform/tenants` 和 `GET /api/platform/tenants/{tenantId}`，使用 `PLATFORM_TENANT_VIEW` 平台权限且不依赖租户成员上下文。
- [x] 2.7 先添加状态管理测试，再实现 `PUT /api/platform/tenants/{tenantId}/status` 的参数校验、幂等更新、停用后恢复和 `PLATFORM_TENANT_STATUS_EDIT` 权限校验。
- [x] 2.8 在实际状态变化的事务中记录 `PLATFORM_UPDATE_TENANT_STATUS` 审计及前后状态详情，并验证重复设置相同状态不重复记录。

## 3. 前端租户管理

- [x] 3.1 在编写 Ant Design 与 ProComponents 页面代码前运行相关组件的 `npx antd info <Component>`，确认 ProTable、Drawer、Descriptions、Tag、Statistic 和 Popconfirm 在当前版本的 API。
- [x] 3.2 新增页面本地类型和服务封装，接入平台租户分页、详情和状态接口，不手工修改 `src/services/ant-design-pro/` 生成目录。
- [x] 3.3 扩展 `access.ts` 及测试，增加 `canViewPlatformTenants` 和 `canEditPlatformTenantStatus`，分别覆盖页面查看与状态操作权限。
- [x] 3.4 在“商业化管理”下新增“租户管理”路由和菜单，并补充现有全部语言文件及路由测试。
- [x] 3.5 先添加页面测试，再实现 ProTable 租户列表、关键字/状态/当前套餐类型筛选、分页字段和无套餐/零余额展示。
- [x] 3.6 实现租户详情抽屉，展示基本信息、Owner、有效成员数、积分余额、当前套餐和按生效时间排序的待生效套餐，并保留列表查询上下文。
- [x] 3.7 实现按权限显示的启用/停用操作、明确的确认文案、取消不请求、成功提示及列表与详情刷新，并补充对应交互测试。

## 4. 验证与交付

- [x] 4.1 运行平台租户相关后端单元与集成测试，确认分页筛选、批量聚合、停用恢复、权限和审计场景通过。
- [x] 4.2 运行新增前端测试与 TypeScript 类型检查，确认路由、权限、列表、详情和启停交互通过。
- [x] 4.3 运行 `npm run lint`、`npx antd lint ./src`、前后端完整测试和生产构建，并修复本变更引入的问题。
- [x] 4.4 使用具有完整权限、仅查看权限和无权限的三类账号完成手工冒烟验证，确认停用租户仍可被平台查询并重新启用。
