# 验证记录

## 2026-09-13 供应商缓存受控验证

在部署服务器上，使用当前部署 JAR、共享环境密钥和已启用的 `gpt-5.6-terra` 配置，向 OpenAI 兼容网关发起三次相同模型、相同缓存键、相同稳定上下文的最小文本请求。探测未创建项目或任务，未写入业务表；提示词、响应正文和密钥均未保留。

| 调用 | 输入 tokens | 缓存输入 tokens | 输出 tokens | 耗时 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| 1 | 4028 | 未知 | 5 | 3259ms | 成功，未截断 |
| 2 | 4028 | 3840 | 5 | 1967ms | 成功，未截断 |
| 3 | 4028 | 3840 | 5 | 1939ms | 成功，未截断 |

- 请求轮数：3；总输入 12084 tokens，已知缓存输入 7680 tokens。
- 已知缓存明细覆盖 2/3 调用；仅对已知调用计算，缓存命中率为 95.33%（7680 / 8056）。
- 成功率：100%（3/3）；模型耗时 P50 为 1967ms，P95 为 3259ms（nearest-rank）。
- 网关未返回 `cache_write_tokens`；首次调用的缓存明细与全部调用的成本未知。探测绕过业务账本，未将推断成本写入调用日志。
