## 1. 引用与清理范围

- [x] 1.1 枚举新旧执行器、业务调度启停配置、旧接口、旧配置源及所有调用者，记录逐项退役/迁移目标，覆盖后台作业和 mutation 返回值。
- [x] 1.2 创建 data-cleanup-manifest.md，列出准确表/字段、业务类型过滤、FK/逻辑引用、删除顺序、保留配置及验证 SQL；将共享账本依赖单独标明。
- [x] 1.3 对照活跃 OpenSpec 变更记录替代关系及待补验证，确定 Markdown 审核为最终目标，避免恢复结构化候选/语义门禁链路。

## 2. 唯一分析执行与逐集调度

- [x] 2.1 增加四阶段仅调用 Workflow Agent 及配置缺失明确失败的契约测试，删除旧 split/summarize/invoke 执行分支。
- [x] 2.2 固定可信逐集上下文/预加载，删除 LEGACY_V1、只作切流用途的 pipelineVersion 字段与前端类型，保留来源指纹和有效审计版本。
- [x] 2.3 验证并补齐拆分后持久化概要/识别工作单元、独立推进与恢复，覆盖概要失败仍推进识别、部分失败不误报完成。
- [x] 2.4 固定资产完成后的自动分镜事件与派发，覆盖重启、重复回调、手工生成竞争、人工成果保护、资金不足和独立重试。
- [x] 2.5 删除已拒绝调用的旧同步分镜方法及孤立调用/测试，保留共享执行与正式 Agent 保存契约。

## 3. 唯一 Markdown 审核

- [x] 3.1 为 QUICK/DEEP 的非空未截断 Markdown 持久化成功条件增加测试，覆盖无终态保存工具也成功以及空/截断输出失败。
- [x] 3.2 删除旧直连审核、结构化问题/命中/跨轮匹配、语义裁决与异常门禁执行器及专用工具，保留最新维度/范围冻结和 Markdown 单元/汇总。
- [x] 3.3 删除旧结构化审核 API、导出和格式分支，迁移前端工作台/项目库到 Markdown 唯一展示、复制、下载、状态和重试。
- [x] 3.4 验证 DEEP 单元失败复用、聚合重试、取消、范围隔离、Run 日志与积分结算，移除仅验证旧协议的测试。

## 4. 资产与工作台接口退役

- [x] 4.1 删除旧 scripts/ai-extract-elements 及候选审核/确认入口、专用 handler/DTO/服务；验证退休接口不映射且不启动付费操作。
- [x] 4.2 验证 scoped reextraction 的 ALL/CHARACTER/SCENE/PROP、预检、提示词策略、匹配收口和人工资产保护，保留合法手工编辑接口。
- [x] 4.3 将返回聚合 workspace 的 mutation 改为最小结果，逐个迁移前端消费者到针对性刷新并测试错误与并发响应。
- [x] 4.4 删除 script-workspace、asset-settings-workspace 聚合 GET、组装器、DTO、旧手写 service 与兼容轮询；验证页面只访问轻量和按需接口。
- [x] 4.5 将概要导航、工具读取、Agent 保存和人工编辑统一到正式概要仓储，删除 summary 镜像双写及旧结果 fallback。

## 5. Agent 与 Skill 配置收敛

- [x] 5.1 迁移仍使用旧 Agent/Skill definition 的创作、改写、提示词等调用者到明确的唯一配置源，验证功能与模型参数解析继续有效。
- [x] 5.2 删除旧 catalog/definition 管理 API、服务、页面、专用权限和路由，将 Workflow Agent/文件 Skill 命名为 Agent 管理、Skill 管理。
- [x] 5.3 更新当前权限映射、菜单和独立读写测试，验证旧权限不会扩大新配置权限，当前 Agent/Skill 编辑与运行日志可用。
- [x] 5.4 清除 bootstrap 切流依赖，验证缺省定义初始化幂等且不覆盖管理员配置，缺少兼容模型可诊断。

## 6. 配置与运行器收敛

- [x] 6.1 删除分析、上下文、自动分镜、分镜、审核 bootstrap/QUICK/DEEP 及旧审核 features 开关、注入参数和条件分支；缓存观测保留为正常能力。
- [x] 6.2 删除共享执行、视频生成、视频拆解及自动分镜等业务调度启停配置，保留延迟/并发/超时/租约参数，确认没有替代布尔开关。
- [x] 6.3 改造调度测试隔离方式，验证不提供退休环境变量时相关 bean 启动且任务可领取，支付/存储/mock 配置不受影响。
- [x] 6.4 清理 env 示例、application 配置、部署脚本和运行手册中的退休变量，记录唯一链路的配置诊断与任务取消方式。

## 7. 开发期数据与结构迁移

- [x] 7.1 按清单实现未完成旧任务的终止、预扣释放及必要账本审计保留，测试无悬空引用和重复释放。
- [x] 7.2 新增后续 Flyway 清理迁移，删除确认无当前消费者的旧专用表/权限/兼容字段和范围内历史记录，保留原迁移文件。
- [x] 7.3 在隔离空库运行全部迁移，验证最终结构与新 Agent/Skill bootstrap。
- [x] 7.4 在隔离的混合新旧数据夹具上升级，验证保留配置、新业务与账本不受损，旧任务不可再次派发，孤立引用为零。
- [x] 7.5 完成目标确认、停机、快照、取消释放、迁移、协调启动与恢复步骤的运行手册；文件清理仅列精确无引用对象键，输出删除计数报告格式。

## 8. 联调与交付

- [x] 8.1 同步后端 OpenAPI 并用项目既有命令再生成前端服务，禁止手改生成目录；检查退休端点不再出现在生成客户端。
- [x] 8.2 使用受控测试提供者验证完整新链路：创建分析、概要失败继续识别、自动分镜、单类型重提取、QUICK/DEEP Markdown、轻量读取和取消恢复。
- [x] 8.3 运行后端相关与完整测试、前端测试、npm run lint、npx antd lint ./src 和生产构建，记录本次结果并处理引入的失败。
- [x] 8.4 扫描当前运行代码、配置、服务、路由及测试，证明无旧执行器、退休开关、旧 API 或兼容双写；迁移历史与历史文档中的文字单独标识。
- [x] 8.5 同步相关活跃变更已被替代的兼容/切流条款并安排规格归档顺序，保留其原验证记录；运行 openspec validate remove-legacy-ai-workflow-paths --strict 并整理实现及部署清单。
