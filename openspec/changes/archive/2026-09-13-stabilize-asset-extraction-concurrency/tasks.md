## 1. 核实基础与数据库约束

- [x] 1.1 审计当前两入口、任务恢复、正式保存及收口路径，与在途变更核对接口，记录事务和执行身份边界。
- [x] 1.2 核实资产、变体、绑定已有唯一索引和身份锁；增加历史重复只读预检，验证不会自动删除或合并数据。
- [x] 1.3 添加剧本级协调记录迁移及必要幂等索引，验证软删除兼容性、重复数据失败行为和升级回滚说明。

## 2. 提交防重与持久化执行协调

- [x] 2.1 实现等价请求指纹和原子 admission，在操作创建及积分预占前复用等价任务或返回冲突任务引用。
- [x] 2.2 将资产重提取和剧本分析资产阶段接入同一剧本协调记录，支持等待调度、不同剧本并发和同剧本互斥。
- [x] 2.3 实现 owner/version/attempt 条件接管、终态释放与孤立记录恢复，阻断旧 worker 写入和释放新拥有者。
- [x] 2.4 按持久化 business_type 区分分析任务和重提取操作，修正操作 ID 用于分析上下文的混用，并验证冻结模型非空。

## 3. 有界目录与检索工具

- [x] 3.1 实现本集候选摘要、明确分页元数据及字节预算，将源片段分类与资产详情加载解耦。
- [x] 3.2 实现 search_script_assets 的规范名/别名检索、keyset 分页、预算及查询绑定游标。
- [x] 3.3 实现 read_asset_details 的批量 key 上限、形态分页及作用域检查，覆盖 51 个以上形态和超长单条内容。
- [x] 3.4 更新工具注册、Agent allowlist、预加载和保存顺序契约；为线上持久化 Skill 编写版本升级，规定候选遗漏时先检索。
- [x] 3.5 验证 215+ 道具目录加载、漏召回后的全局查询、游标隔离、提示词状态以及共享读取工具的分镜兼容性。

## 4. 写入幂等与安全收口

- [x] 4.1 在正式保存事务内校验协调所有权、当前 attempt 与源指纹，复用现有身份锁并统一全局精确名称/别名匹配。
- [x] 4.2 核实并补齐变体、剧集绑定和提交证据幂等，提交后崩溃恢复复用证据而不重新生成已完成单元。
- [x] 4.3 将收口改为校验全部冻结单元提交证据、版本及拥有权的幂等事务，保护手动资产和范围外数据。

## 5. 前端任务复用与冲突反馈

- [x] 5.1 接入复用任务与冲突响应，显示有权限查看的任务入口并恢复轮询，保留原有 preflight 与提示词策略确认。
- [x] 5.2 验证重复点击、不同策略/范围冲突、刷新恢复及成功/失败/取消后清除 loading，不自动重提付费任务。

## 6. 并发与发布验收

- [x] 6.1 使用真实 MySQL 事务并发测试验证不同客户端 key 只产生一个任务和预占、不同用户隔离、跨入口互斥及不同剧本并发。
- [x] 6.2 验证同名并发创建、明确别名复用、歧义拒绝、形态绑定去重及两种提示词策略，断言最终业务记录而非仅 SQL 文本。
- [x] 6.3 注入保存提交后崩溃、执行取消、租约接管、旧 worker 返回、源版本变化及收口重试，验证无重复提交或误清理。
- [x] 6.4 运行后端相关测试、前端类型检查和项目规定 lint，记录检查结果及未覆盖边界。
- [x] 6.5 编写线上发布/回滚和项目 33 验收步骤：排空旧执行、同步工具与 Skill、检查迁移、观察各集正式保存和最终资产数量；付费线上验收在发布阶段执行。
