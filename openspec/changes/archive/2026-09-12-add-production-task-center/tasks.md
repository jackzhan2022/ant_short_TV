## 1. 来源盘点与契约

- [x] 1.1 核验全部生产类型的创建、异步执行和结果链路，记录来源表、稳定 ID、批次关系、权威状态、权限、结果入口及非异步差距的覆盖矩阵。
- [x] 1.2 明确软删除/归档来源的保留策略、历史无父批次记录展示与每类型状态映射，保证不伪造可恢复后台行为。
- [x] 1.3 定义 typed taskKey、分页摘要、summary、详情、分页子项、nullable progress、allowedActions 和重新生成响应契约。

## 2. 服务端权限边界

- [x] 2.1 实现当前有效团队成员及 OWNER/有效系统 ADMIN 判定，并提供服务端 canViewTeamTasks 能力。
- [x] 2.2 实现 mine 创建人约束、team 角色约束及统一详情/子项/统计可见性策略，验证子项真实属于父任务。
- [x] 2.3 实现项目访问撤销后的受限本人摘要、未绑定项目任务保护及业务结果链接独立授权。
- [x] 2.4 增加跨团队、跨成员、伪造 creatorId/scope、自定义同名 ADMIN、角色停用/撤销、成员移除及所有权转让的授权集成测试。

## 3. 聚合查询与来源适配

- [x] 3.1 实现静态来源查询视图或派生表及服务端筛选、计数和分页，不拉取完整结果正文；按查询计划添加必要索引。
- [x] 3.2 接入拆剧批次/分集和分镜批次/单集，验证顶层去重与分页子项。
- [x] 3.3 接入剧本分析、审核及已持久化 Script AI Operation 类型，保留重试/再生成 lineage，排除内部调用记录。
- [x] 3.4 接入图片和视频任务，处理真实批次关联、独立历史记录及目标资产可用性。
- [x] 3.5 接入符合异步条件的语音、镜头合成与单集合成来源，验证无 AI execution 也能查询；将真实执行能力缺口明确记录，不宣称未完成来源已覆盖。
- [x] 3.6 实现所有状态分组、批次混合结果、计数一致性、无数值进度、成功带警告和结果摘要映射。
- [x] 3.7 提供列表、summary、选中详情、分页子项端点，批量富化当前页标题/创建人/权限/结果入口。
- [x] 3.8 使用多类型真实数据库测试验证排序、筛选、总数、摘要、历史记录、非 AI 类型和批次去重，检查查询数不随列表行数线性增长。

## 4. 本人任务控制

- [x] 4.1 按来源实现 allowedActions，组合本人、当前业务权限、状态与处理器支持；其他成员任务仅返回读取动作。
- [x] 4.2 实现中心 cancel/retry/regenerate 入口并委派既有领域服务，操作时重验权限和状态，保留计费、幂等、结果与审计语义。
- [x] 4.3 测试所有者/管理员对他人任务控制被拒、本人业务权限被撤销、状态竞争、重复再生成请求以及失败子项重试不重跑成功兄弟项。

## 5. 前端任务中心

- [x] 5.1 实现 /tasks 路由、我的分组菜单和国际化标签，使用服务端能力控制团队页签，默认 mine。
- [x] 5.2 实现分页列表、状态计数、类型/状态/项目/时间筛选及团队创建人筛选，保留 URL 查询状态和清晰空态/错误重试。
- [x] 5.3 实现单任务详情抽屉、分页子任务、真实进度/阶段、结果入口及仅允许的本人操作；不提供全局新建/删除/归档。
- [x] 5.4 实现可见查询轮询、终态停止、页面卸载清理、团队切换重置与迟到响应隔离。
- [x] 5.5 增加前端测试，验证角色视图、直接团队 URL 被拒、无每行详情请求、深链接恢复、跨团队响应隔离、终态轮询停止和动作可见性。

## 6. 验收与交付

- [x] 6.1 逐项对照覆盖矩阵完成真实来源验收，确认所有已存在的耗时异步生产任务均可查询，列明同步/未实现来源并提出后续执行改造边界。
- [x] 6.2 运行受影响后端测试、前端测试、npm run lint 和 npx antd lint ./src；按仓库约定在编写 antd 组件前查询其 API，生成服务仅通过既有 OpenAPI 流程更新。
- [x] 6.3 验证增量查询结构迁移、历史数据读取及回滚后既有业务入口正常，记录性能证据和部署/回滚说明。
- [x] 6.4 运行 OpenSpec 严格校验，核对规格场景与实施证据；未通过的来源或测试保持未完成状态。
